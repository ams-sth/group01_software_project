databases:
  - name: splitsync-db
    plan: free
    databaseName: splitsync
    user: splitsync

services:
  - type: web
    name: splitsync-api
    runtime: docker
    plan: free
    dockerfilePath: ./splitsync/server/Dockerfile
    dockerContext: ./splitsync/server
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: ConnectionStrings__Default
        fromDatabase:
          name: splitsync-db
          property: connectionString
      - key: Jwt__Issuer
        value: SplitSync.Api
      - key: Jwt__Audience
        value: SplitSync.Client
      - key: Jwt__Key
        generateValue: true
      - key: Cors__AllowedOrigin
        value: https://splitsync-client.onrender.com

  - type: web
    name: splitsync-client
    runtime: static
    buildCommand: cd splitsync/client && npx --yes pnpm@11 install --frozen-lockfile && npx --yes pnpm@11 build
    staticPublishPath: ./splitsync/client/dist
    envVars:
      - key: VITE_API_URL
        value: https://splitsync-api-4qx2.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
